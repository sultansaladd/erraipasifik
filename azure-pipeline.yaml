pool:
  vmImage: 'ubuntu-latest'
strategy:
  matrix:
    node_12_x:
      node_version: 12

steps:
- task: NodeTool@0 
  inputs:
    versionSpec: $(node_version)
- task: Npm@1
  inputs:
  command: 'install'

- script: npm run compile